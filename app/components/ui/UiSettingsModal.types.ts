/** A section in the settings modal navigation */
export interface SettingsSection {
  /** Unique identifier for the section */
  id: string
  /** Display label shown in the navigation */
  label: string
  /** Lucide icon name shown before the label */
  icon: string
}
